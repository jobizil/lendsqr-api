// import db from "../../database";
// import bcrypt from "bcrypt";

// const createUserDoa = async ({ username, email, password }: { username: string; email: string; password: string }) => {
// 	const [id]: any = await db("users").insert({
// 		username,
// 		email,
// 		password: bcrypt.hashSync(password, 10),
// 	});
// 	return id;
// };

// export default createUserDoa;

//Data Access Object (doa)
